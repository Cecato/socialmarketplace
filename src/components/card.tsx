import Image from 'next/image';

type Props = {
    img: string;
    alt: string;
    title: string;
    text: string;
}

export default function Card({img, alt, title, text}: Props){
    return(
        <>
        <div>
            <div>
            </div>
            <div>

            </div>
        </div>
        </>
    )
}